// Array
var arr = [1,2,3];
var sum = 0;

// Convert to combinations
// convert to binary
// Calculate diff
// Sum diff

// for(var i=0;i<arr.length;i++){
//     for(var j=i+1;j<arr.length;j++){
//         if(arr[i] !== arr[j]){
//             var diff = ((arr[i]^arr[j]).toString(2).match(/1/g) || 0).length;
//             sum+=diff;
//         }
//     }
// }
// console.log(sum);

for(var i=0;i<32;i++){
    var set = 0;
    for(var j=0;j<arr.length;j++){
        // set = 
    }
}

// console.log(num.toString(2).padStart(4,'0'));

// var diff = ((num ^ num2).toString(2).match(/1/g) || []).length;
// console.log(diff);

